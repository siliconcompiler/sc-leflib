[build-system]
requires = [
    "setuptools >= 80",
    "scikit-build >= 0.12",
    "wheel",
    "cython",
    "cmake"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = [
    "tests"
]
timeout = "120"
addopts = [
    "--import-mode=importlib",
]
